From 375460aeb469757b84c3867669b741be7b58fbb7 Mon Sep 17 00:00:00 2001
From: Cemil Coskun <ccoskun@codeaurora.org>
Date: Fri, 13 Dec 2019 09:28:20 -0800
Subject: [PATCH] net: skbuff_debug: Fix compiler warning for const

Change-Id: I80fdeed31a472ad455336589983beedd52eff5b7
Signed-off-by: Cemil Coskun <ccoskun@codeaurora.org>
---
 net/core/skbuff_debug.c | 5 ++---
 1 file changed, 2 insertions(+), 3 deletions(-)

--- a/net/core/skbuff_debug.c
+++ b/net/core/skbuff_debug.c
@@ -276,7 +276,7 @@ void skbuff_debugobj_print_skb_list(cons
 				    const char *list_title, int cpu)
 {
 	int count;
-	const struct sk_buff *skb_i;
+	struct sk_buff *skb_i = (struct sk_buff *)skb_list;
 	u32 sum_i, sum_now;
 	int obj_state;
 
@@ -285,12 +285,11 @@ void skbuff_debugobj_print_skb_list(cons
 		put_cpu();
 	}
 	pr_emerg("skb_debug: start skb list '%s' [CPU#%d]\n", list_title, cpu);
-	skb_i = skb_list;
 	count = 0;
 	if (skb_list) {
 		do {
 			obj_state =
-				debug_object_get_state((struct sk_buff *)skb_i);
+				debug_object_get_state(skb_i);
 			if (obj_state < ODEBUG_STATE_NOTAVAILABLE) {
 				sum_i = skb_i->sum;
 				sum_now = skbuff_debugobj_sum(skb_i);
